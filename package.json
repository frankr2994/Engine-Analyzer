{
  "name": "engine-analyzer",
  "version": "1.0.0",
  "description": "Modular internal combustion engine simulation architecture",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.8.2",
    "vitest": "^4.1.11"
  }
}
